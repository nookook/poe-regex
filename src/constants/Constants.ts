export const MINIMUM_AMOUNT_OF_CHARACTERS = 4; // start search at 4 character long matches to avoid false matches from randomly generated map names in the game
export const ERROR_NO_REGEX_MATCH_FOUND = "ERROR! failed to find regex match";
export const REGEX_CHARACTERS_TO_IGNORE_IN_FILTER_OPTIONS = `[0-9\-\(\)\n]`;
